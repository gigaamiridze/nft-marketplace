import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  ethPriceContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    columnGap: 2,
  },
  ethImage: {
    width: 20,
    height: 20,
  },
});
